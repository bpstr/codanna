# Deep-review remediation — 2026-09-12

Reviewed base: `1b59d9b8a2f9d20204faea66671712dff66e6c75` in `bpstr/codanna`.

This is a bounded remediation batch, not a claim that all 38 findings are fixed or
that the original severity/confidence labels establish runtime reachability.
Changes stay in this fork. No upstream issues or pull requests are created.

## Important triage corrections

- Finding 3 is reproducible in the public standalone `EmbedStage::embed_and_store`
  API. The ordinary phase-one indexing pipeline currently instantiates
  `SemanticEmbedStage`, not that stage. The regression test covers the actual
  affected API; this finding alone is not evidence of data loss in a user's
  existing normal CLI index.
- Finding 21 concerns the public compact-symbol conversion. Normal indexing
  predominantly uses `Symbol`; the conversion defect does not prove that every
  repository with more than 65,535 files has corrupted its main index.
- Finding 28 bundled several error paths. The reviewed base already propagated
  explicit final `commit`/`flush` failures. Per-record/start/automatic-commit
  failures in `WriteStage::write` were still swallowed; those now propagate.
- Network, plugin-installation, and generated-page issues require different
  exposure conditions. They are serious boundary defects, not eleven identical,
  unconditional P0 failures in a local stdio deployment.

## Deployment and API compatibility

### Network MCP is a single-operator authenticated service

Both HTTP and HTTPS now require an operator-provided `CODANNA_MCP_TOKEN` with
32–512 ASCII bearer-token characters. Generate a random secret, do not use a
sample token, commit it, print it in logs, or share it with an untrusted workspace.
Configure clients to send `Authorization: Bearer <secret>` on MCP requests.
Use a secret manager or an untracked environment file with restricted access.

For local setup, a shell can generate the secret without printing it:

```sh
export CODANNA_MCP_TOKEN="$(python3 -c 'import secrets; print(secrets.token_urlsafe(32))')"
# Launch the existing HTTP/HTTPS serve command from this environment.
# Separately configure the same secret in the authorized client's private settings.
```

Plain HTTP can bind only to a loopback IP. Non-loopback service requires HTTPS.
The token is checked on both transports; TLS is not treated as client identity.
Only `/health` remains unauthenticated. The placeholder OAuth authorization,
registration, token, and discovery endpoints are removed rather than repaired
into a pretend authorization server. OAuth-only clients will need a proper
external authorization adapter or a future standards-compliant implementation.

This is **not** multi-user identity, per-workspace ACLs, or OAuth scope enforcement.
A token holder is trusted to use the configured index and its mutations. Use
separate processes/indexes/secrets for separate trust domains. Do not expose a
shared installation to mutually untrusted tenants.

Stdio does not require a network token and is unchanged at that boundary.
The optional, unpinned Context7 `npx` entry is removed from `.mcp_stdio.json`;
users needing that separate tool should install and audit an exact version under
their own lockfile, rather than restoring a mutable automatic download.

### Files, state, and public Rust APIs

Stop every older Codanna server using an index before upgrading. The serve lock
now uses an OS-held file lock; its on-disk file intentionally remains after exit.
Do not unlink an active lock file to force a second server to start.

Invalid/unreadable registry and index metadata now cause errors rather than being
silently replaced. Preserve the file, diagnose the error, and perform explicit
recovery. This batch does not implement a concurrent registry transaction manager.

Plugin clones containing any symlink or special file are rejected, including
internal symlinks. This is intentionally stricter than copying a dereferenced
plugin tree. Existing plugins relying on symlinks must ship regular files.

Search requests accept limits from 1 to 1000; impact depth is 1 to 10. Invalid
parameters return errors, not silent clamping. Internal full-index enumeration
is not capped at 1000; it bounds allocation by actual indexed document count.

The crate-root `StorageError`/`StorageResult` now refer to the active storage API.
The older types remain available as `LegacyStorageError`/`LegacyStorageResult`
and under `error`. Consumers matching old root variants must update their imports.
`CompactSymbol.file_id` is now `u32`; the structure remains 32 bytes, but callers
must not assume the old field offsets are a stable binary persistence format.

`Chunker::chunk` accepts `ValidatedChunkingConfig`. External/Serde settings remain
`ChunkingConfig`; convert with `ValidatedChunkingConfig::try_from(config)` after
merging collection overrides. Invalid settings are rejected before collection
mutation. The existing validation contract requires `overlap < min < max`.

`WriteStage::write` now returns a `StorageResult<WriteStats>`; callers must handle
errors. Its accepted-write count is not a durability receipt until commit/flush
succeeds. Phase two no longer returns successful resolution stats after one of
these writes fails. Already committed work is not rolled back across a failed
multi-batch operation.

Profile removal preserves the lock entry when a tracked file cannot be removed.
Some earlier files may already be deleted; retry is supported. Team sync attempts
remaining profiles but returns an error containing failed entries.

### Visualization compatibility

Repository data is encoded separately for HTML and JavaScript contexts. Generated
pages receive a restrictive CSP, and executable paths/arguments no longer go
through a shell. On Windows, browser URLs are printed for manual opening instead
of invoking the `cmd.exe` `start` builtin.

The Node security suite was exercised with Node 22.16.0. Browser rendering,
accessibility, and every older Node/Codanna version are not certified by that
suite. Do not interpret injection-test success as a complete interactive UI test.

## Finding-by-finding disposition

“Fixed” describes the reported defect in this batch, not comprehensive safety of
its entire subsystem. “Partial” identifies an explicit narrower mitigation.
“Open” is a concern that still needs implementation or deeper validation.

| # | Disposition | Evidence / scope of this batch |
|---|---|---|
| 1 | Fixed | Lifetime OS lock; no stale-owner unlink or ownership-blind removal; two-contender regression. |
| 2 | Fixed | Unique same-directory staging files, sync and atomic promotion; independent-writer inode regression. |
| 3 | Fixed, reachability qualified | One publication after all mock-embedded chunks succeed; 257 symbols and short second response tested. Standalone stage, not current normal phase-one stage. |
| 4 | Partial | Mandatory constant-time checked bearer secret on both transports; loopback-only cleartext. Single-operator isolation, not identity/scopes/tenant ACLs. |
| 5 | Fixed | Only missing registry is empty; corrupt/unreadable/version-incompatible registry preserved; atomic save. |
| 6 | Fixed by removal | Unsafe placeholder OAuth pages/endpoints removed; their parameter interpolation and redirects are unavailable. |
| 7 | Partial | Canonical configured-root containment, validate all paths before mutation, reject symlink escapes, at most 64 paths. No total traversal quota or separate mutation scope. |
| 8 | Fixed | Removed credential/header/body/token/OAuth payload diagnostics from the affected transport handlers. |
| 9 | Fixed | Preflight cloned trees and reject symlinks/special entries before extraction, including whole-repo plugins. |
| 10 | Fixed, browser QA outstanding | Context-safe repository strings and final-page CSP. Actual generators tested with hostile fixture data; no full browser interaction certification. |
| 11 | Fixed | `execFileSync` with argv for binary overrides and symbol queries; literal metacharacter filename regression. |
| 12 | Partial | Force-reindex moves blocking work off Tokio workers and rejects an occupied facade. Search/watcher/semantic lock architecture remains open. |
| 13 | Open | Per-edit repository-wide `SymbolLookupCache::from_index` still exists. Needs incremental cache lifecycle and scaling measurements. |
| 14 | Open | Semantic persistence still clones/rewrites the corpus. Needs dirty/delta persistence and crash/recovery tests, not a blind “skip save”. |
| 15 | Open | Count-first full relationship drains and repeated hydration still have scaling costs. Need bounded query/frontier APIs without restoring prior silent truncation. |
| 16 | Fixed, transport witness outstanding | HTTPS factory builds a distinct server/peer per session; shared notification loop tested with independent owners. Multi-client TLS wire witness remains to add. |
| 17 | Fixed at session ownership boundary | Listener owns only peer/receiver/token, cancels when server session owner is dropped, and terminates on send failure/timeout. Reconnect/expiry behavior still needs a full transport witness. |
| 18 | Open | Unknown-language fallback and poisoned registry unwrap remain in behavior construction; typed error propagation through context building needs a separate change. |
| 19 | Fixed, API change | Root exports match active storage errors; legacy aliases retained; compiled type-identity regression. |
| 20 | Fixed | Legacy parser construction delegates to registry after configuration checks; enabled-language completeness test. |
| 21 | Fixed, reachability qualified | Full-width IDs survive both compact conversion paths, including 65,536 and `u32::MAX`; size stays 32 bytes. |
| 22 | Fixed, API change | Runtime-only validated chunking type; boundary validation before index mutation; invalid settings preserve existing state. |
| 23 | Open | TypeScript tests still mutate process CWD under different/no locks. Prefer explicit resolver roots; merely locking one test module is insufficient. |
| 24 | Open for legacy scripts | New regression suite is explicitly gated in CI and rejects zero-test filters; four legacy shell scripts remain ungated/non-authoritative. |
| 25 | Fixed for request limits | Bounds enforced in deserialization and direct handlers; storage rejects invalid search limits and protects zero/huge enumeration collectors. Graph breadth budgeting remains with #15. |
| 26 | Fixed | Delete only the target collection's file states; preserve other collections through reopen and no-op reindex. |
| 27 | Fixed by opt-in removal | Mutable Context7 download and unauthenticated network example removed from default MCP configuration. |
| 28 | Fixed | Start/store/automatic-commit errors now reach phase two; unavailable writer and failed flush preserve honest failure/pending state. |
| 29 | Fixed | Failed profile deletion preserves retry metadata; mixed-success sync returns a nonzero error outcome. |
| 30 | Fixed for affected delivery paths | Check notification results, stop failed/timed-out listeners, resync on lag, and reject custom notification without a peer. |
| 31 | Open | FileWalker still drops traversal errors. Needs result-bearing discovery APIs and watcher/index caller migration. |
| 32 | Fixed | Preserve corrupt/unreadable metadata on load/save; HTTP/HTTPS no longer replace a failed facade load with a fresh one. |
| 33 | Example-only, unchanged | Narrow mail screenshot layout is present in the React sample/indexing corpus; not a Codanna service shutdown defect. |
| 34 | Example-only, unchanged | Sample theme swatches lack accessibility semantics; not changed during backend/security remediation. |
| 35 | Partial | Removed obsolete embedding architecture example and stale factory support prose; other bundled documentation claims are not declared repaired. |
| 36 | Open / not fully revalidated | New tests exercise affected production entrypoints with deterministic fixtures. The original report's broader weak-oracle set is not fully repaired. |
| 37 | Partial documentation only | Tested Node runtime and remaining browser/runtime caveats documented here. Uniform plugin preflight/minimum-version declarations remain open. |
| 38 | Open design risk | Legacy parser now delegates to registry, but language/tool catalogs are not fully centralized. No speculative registry rewrite included. |

## Reproducible validation

Run from the repository root with Rust stable and Node 22.16.0:

```sh
bash contributing/scripts/review-regressions.sh
```

The script compiles all test targets, runs the `hardening_review_` regressions,
runs existing unit suites in the affected deterministic subsystems, exercises
real local HTTP sessions with semantic indexing disabled, and runs the Node
injection/argv fixtures. It fails on empty Rust test filters. No package install,
model inference service, paid endpoint, or live repository clone is used by these
tests; ordinary Cargo dependency acquisition remains a build prerequisite.

The tested source patch passed in Actions run `34704016905`: 99 distinct Rust tests
(31 new review regressions, 63 other unit tests, and five HTTP transport tests),
plus five Node security tests. Module reruns cause 110 Rust test executions; that
is not 110 distinct tests. Two existing network-clone tests remained ignored.

`.github/workflows/review-regressions.yml` provides the persistent PR gate.
The review workbench's source-transfer files are deliberately not part of this
patch. Linux CI results attest only the exact tested patch. Existing repository-wide
CI, macOS/Windows witnesses, TLS multi-session testing, browser interaction,
permission-denied traversal, crash/power-loss behavior and large-corpus performance
are not substituted by these targeted tests. Consult the attached CI logs for
actual executed counts; compilation alone is not reported as test execution.
