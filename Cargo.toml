[package]
name = "codanna"
version = "0.16.0"
authors = ["Angel Bartolli <bartolli@gmail.com>"]
edition = "2024"
description = "Local code intelligence MCP server and CLI for AI coding agents"
documentation = "https://docs.codanna.sh"
homepage = "https://docs.codanna.sh"
repository = "https://github.com/bartolli/codanna"
license = "Apache-2.0"
readme = "README.md"
keywords = ["code-indexing", "code-intelligence", "mcp-server", "semantic-search", "ai-agents"]
categories = ["development-tools", "command-line-utilities", "parsing", "text-processing", "compilers"]
autoexamples = false
exclude = [".github/", "contributing/", "tests/", "docs/", "agents/", ".claude-plugin/", "assets/"]

[package.metadata.binstall]
pkg-fmt = "txz"
bin-dir = "{ name }-{ version }-{ target-family }/{ bin }{ binary-ext }"

[package.metadata.binstall.overrides.x86_64-unknown-linux-gnu]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ version }-linux-x64.tar.xz"
bin-dir = "{ name }-{ version }-linux-x64/{ bin }"

[package.metadata.binstall.overrides.x86_64-apple-darwin]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ version }-macos-x64.tar.xz"
bin-dir = "{ name }-{ version }-macos-x64/{ bin }"

[package.metadata.binstall.overrides.aarch64-apple-darwin]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ version }-macos-arm64.tar.xz"
bin-dir = "{ name }-{ version }-macos-arm64/{ bin }"

[package.metadata.binstall.overrides.x86_64-pc-windows-msvc]
pkg-url = "{ repo }/releases/download/v{ version }/{ name }-{ version }-windows-x64.zip"
pkg-fmt = "zip"
bin-dir = "{ name }-{ version }-windows-x64/{ bin }{ binary-ext }"

[dependencies]
anyhow = "1.0.104"
bitflags = "2.13.1"
clap = { version = "4.6.6", features = ["derive", "color", "env"] }
crossbeam-channel = "0.5.16"
dashmap = "6.2.1"
dirs = "6.0.0"
figment = { version = "0.10.19", features = ["toml", "env"] }
ignore = "0.4.33"
memmap2 = "0.9.11"
notify = "8.2.0"
num_cpus = "1.17.0"
parking_lot = "0.12.5"
rayon = "1.12.0"
rmcp = { version = "3.1.4", features = ["server", "client", "transport-io", "transport-child-process", "transport-streamable-http-server", "transport-worker"] }
serde = { version = "1.0.229", features = ["derive", "rc"] }
serde_json = "1.0.151"
sha2 = "0.11.0"
hex = "0.4"
tantivy = "0.26.1"
thiserror = "2.0.20"
tokio = { version = "1.53.1", features = ["full"] }
toml = { version = "1.1.4", features = ["preserve_order"] }
tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
tree-sitter = "0.26.13"
tree-sitter-go = "0.25.0"
tree-sitter-gdscript = "6.1.0"
tree-sitter-java = "0.23.5"
tree-sitter-javascript = "0.25.0"
tree-sitter-kotlin-codanna = "0.3.9"
tree-sitter-php = "0.24.1"
tree-sitter-python = "0.25.0"
tree-sitter-rust = "0.24.2"
tree-sitter-typescript = "0.23.2"
walkdir = "2.5.0"
# Pinned: fastembed >=5.7 uses ort 2.0.0-rc.11 (ONNX Runtime 1.23) whose
# prebuilt binaries require glibc 2.38+. Homebrew Linux CI runs Ubuntu 22.04
# (glibc 2.35), so the bottle build fails with undefined __isoc23_strtol.
# Upgrade when ort ships glibc 2.35 binaries or Homebrew moves to Ubuntu 24.04.
fastembed = "=5.6.0"
rand = "0.10.2"
indicatif = "0.18.6"
comfy-table = "7.2.2"
console = "0.16.4"
owo-colors = "4.3.0"
axum = { version = "0.8.9", optional = true }
tower = { version = "0.5.3", optional = true }
tower-http = { version = "0.7.0", features = ["cors"], optional = true }
serde_urlencoded = "0.7"
tokio-util = "0.7.19"
axum-server = { version = "0.8.0", features = ["tls-rustls-no-provider"], optional = true }
rustls = { version = "0.23.43", default-features = false, features = ["ring"], optional = true }
rcgen = { version = "0.14.9", optional = true }
is-terminal = "0.4.17"
regex = "1.13.1"
tree-sitter-c = "0.24.2"
tree-sitter-c-sharp = "0.23.5"
tree-sitter-cpp = "0.23.4"
chrono = "0.4.45"
tempfile = "3.27.0"
serde_json5 = "0.2.1"
tree-sitter-swift = "0.7.3"
tree-sitter-lua = "0.5.0"
tree-sitter-clojure-orchard = "0.2.8"
glob = "0.3.4"
async-trait = "0.1.92"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
sysinfo = "0.39.6"
indexmap = { version = "2.14.0", features = ["serde"] }

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
ort = { version = "=2.0.0-rc.10", default-features = false }
testcontainers = "0.28.0"
thread-id = "5.1.0"

[features]
default = ["http-server"]
http-server = ["axum", "tower", "tower-http"]
https-server = ["http-server", "axum-server", "rustls", "rcgen"]
axum = ["dep:axum"]
tower = ["dep:tower"]
tower-http = ["dep:tower-http"]
axum-server = ["dep:axum-server"]
rustls = ["dep:rustls"]
rcgen = ["dep:rcgen"]

[profile.release]
# opt-level = 3
# lto = true
# codegen-units = 1
# strip = true

[[example]]
name = "simple_progress_demo"
path = "examples/simple_progress_demo.rs"

[[example]]
name = "dump_edges"
path = "examples/dump_edges.rs"

[[example]]
name = "pipeline_metrics_demo"
path = "examples/pipeline_metrics_demo.rs"

[[example]]
name = "parser_census"
path = "examples/parser_census.rs"

[[example]]
name = "resolution_precision"
path = "examples/resolution_precision/main.rs"

[[bench]]
name = "unified_output_bench"
harness = false

[[bench]]
name = "kotlin_parser_bench"
harness = false

[target."cfg(windows)".dependencies]
dunce = "1.0.5"
