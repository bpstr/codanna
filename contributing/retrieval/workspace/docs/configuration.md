# Configuration contract

The [deployment limit](../config/limits.toml) calls the setting `blob_cap_bytes`.
It must agree with the code constant, despite the different name. Calendar week
start is unrelated to payload admission. This file alone does not prove wiring.
