# Ambiguous validation

The short name `validate` occurs in contact validation and picture validation.
This document deliberately does not choose either implementation. An unqualified
mention is not enough evidence to attach a unique code dependency.
The inline configuration literal `unrecognised_option` is not a broken symbol.
[External reference](https://example.invalid/reference) is not fetched or resolved.
